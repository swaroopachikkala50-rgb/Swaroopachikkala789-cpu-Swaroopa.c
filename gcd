#include<stdio.h>
int Gcd(int,int);
main()
{
	int n1,n2;
	printf("enter 2 numbers");
	scanf("%d%d",&n1,&n2);
	printf("the Gcd of %d and %d is %d",n1,n2,Gcd(n1,n2));
}
int Gcd(int n1,int n2)
{
	if(n2!=0)
	return Gcd(n2,n1%n2);
	else
	return n1;
}
