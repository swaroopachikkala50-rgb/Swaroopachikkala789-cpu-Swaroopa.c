//program to enter marks from user
#inculde<stdio.h>
main()
{
	int i,marks[6];
	for(i=0;i<=5;i++)
	{
		printf("enter sub marks %d",i+1);
		scanf("%d",&marks[i]);
	}
	printf("the marks are:");
	for(i=0;i<=5;i++);
	{
		printf("%d\t",marks[i]);
	}
}
